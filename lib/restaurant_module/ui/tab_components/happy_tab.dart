import 'package:flutter/material.dart';

class HappyHoursTab extends StatelessWidget {
  const HappyHoursTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Happy Hour Tab"));
  }
}
