import 'package:flutter/material.dart';

class BeerTab extends StatelessWidget {
  const BeerTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Beer Tab"));
  }
}
