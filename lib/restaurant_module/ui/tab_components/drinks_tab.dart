import 'package:flutter/material.dart';

class DrinksTab extends StatelessWidget {
  const DrinksTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Drinks Tab"));
  }
}
